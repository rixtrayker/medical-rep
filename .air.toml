# .air.toml

root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o bin/app ./cmd/main.go"
  bin = "bin/app"
  full_bin = "true"
  log = "build.log"
  exclude_dir = ["tmp", "vendor", "bin"]

[log]
  time = true

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"
